# secrets.auto.tfvars
# This file is automatically loaded by Terraform.
# DO NOT COMMIT THIS FILE TO VERSION CONTROL.
#
# Prefer AppRole + scripts/with-vault-token.sh (or a short-lived VAULT_TOKEN /
# TF_VAR_vault_token in the environment / .env). A long-lived vault_token in
# auto-loaded tfvars is higher risk and is not the recommended path.

vault_address = "https://vault:8200"
# vault_token   = "hvs.REPLACE_WITH_SHORT_LIVED_TOKEN_ONLY_IF_APPROLE_UNAVAILABLE"
