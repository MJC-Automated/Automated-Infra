# Optional overrides. Without this file, tracked non-secret settings are used
# and the Grafana admin password is generated under files/ with mode 0600.

OBS_GRAFANA_ADMIN_USER=admin
OBS_GRAFANA_ADMIN_PASSWORD=<set-strong-password>
OBS_PROMETHEUS_RETENTION=15d
OBS_MANAGE_FIREWALL=true
